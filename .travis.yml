sudo: required

services:
  - docker

before_install:
  - docker build -t psalgaocar/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true psalgaocar/docker-react npm run test -- --coverage